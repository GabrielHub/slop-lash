# Neon Postgres (auto-injected by Vercel + Neon integration, or run `npx vercel env pull .env.local`)
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/dbname?sslmode=require"
DATABASE_URL_UNPOOLED="postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require"

# AI Gateway API Key
AI_GATEWAY_API_KEY="your-api-key-here"

# Host Secret (password to create games)
HOST_SECRET="change-me-to-something-secret"
