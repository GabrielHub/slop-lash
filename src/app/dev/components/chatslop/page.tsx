import { DevChatslopPlayground } from "@/dev/chatslop-components-playground";

export const metadata = {
  title: "Dev Components — ChatSlop",
};

export default function DevChatslopComponentsPage() {
  return <DevChatslopPlayground />;
}
